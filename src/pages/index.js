import Layout from '@/components/layout'

export default function MyApp({ Component, pageProps }) {
  return (
    <Layout>
      {/* <Component {...pageProps} /> */}
      <h1>HI</h1>
    </Layout>
  )
}